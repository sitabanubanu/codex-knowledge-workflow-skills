interface:
  display_name: "Knowledge Workflow Console"
  short_description: "Routes targets through four trusted workflow stages."
  default_prompt: "Use $knowledge-workflow-console to select the analysis target, run the required stages, and report current provenance."
